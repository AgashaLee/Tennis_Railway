__pycache__/
*.pyc

# Secrets — never commit. Set ODDS_API_KEY in Railway env vars instead.
odds_api_key.txt

# Runtime caches and logs (regenerated by the app)
data/odds_*.json
data/tennis_predictions_log.json
